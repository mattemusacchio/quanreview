[project]
name = "quanreview"
version = "0.1.0"
description = "Blind pairwise review of model-extracted quantity events against ground truth"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "eel>=0.18.1,<0.19.0",
    "gevent>=25.5.1,<26.0.0",
    "gevent-websocket>=0.10.1,<0.11.0",
    "pyyaml>=6.0",
]
